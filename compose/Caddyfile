{
  email ops@shiftbloom.studio
}

{$DOMAIN} {
  encode gzip zstd

  @protected {
    path / /api/* /chat* /dashboard* /workflows*
  }

  basic_auth @protected {
    {$CADDY_BASIC_AUTH_USER} {$CADDY_BASIC_AUTH_HASH}
  }

  reverse_proxy app:{$PORT}
}
