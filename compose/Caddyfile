{
  email {$ACME_EMAIL}
}

{$DOMAIN} {
  encode gzip zstd

  @protected {
    path / /api/* /mcp-servers* /skills* /instructions*
  }

  basic_auth @protected {
    {$CADDY_BASIC_AUTH_USER} {$CADDY_BASIC_AUTH_HASH}
  }

  reverse_proxy app:3000
}
