# Shiftbloom Repository Instructions

- Use repository-local `.archon/` workflows before inventing one-off process.
- Use the Shiftbloom team skills that match the task: backend, frontend, devops, pr-review.
- Keep changes small, reviewable, and aligned with the repository's validation commands.
- Treat `github` and `postgres_ro` as canonical MCP profile names.
- Never aim `postgres_ro` at the Archon metadata database.

