# Shiftbloom Global Claude Instructions

- Use Shiftbloom team skills before ad hoc process.
- Prefer repository-local `.archon/` workflows when available.
- Treat `github` and `postgres_ro` as the canonical MCP profile names.
- Archon node-level MCP attachment is currently Claude-first; keep workflow MCP definitions aligned with the repo template.
- Never use the Archon metadata database as a general-purpose MCP target.

