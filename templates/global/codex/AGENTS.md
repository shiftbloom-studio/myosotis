# Shiftbloom Global Codex Instructions

- Use the Shiftbloom skills before inventing local process.
- Prefer repository-local `.archon/` workflows when they exist.
- Treat `github` and `postgres_ro` as the canonical MCP profile names.
- Never point `postgres_ro` at the Archon metadata database.
- Favor direct, reviewable changes and keep repo-specific instructions in the target repository's `AGENTS.md`.

